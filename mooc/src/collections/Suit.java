package collections;

public enum Suit {
	DIAMOND, SPADE, CLUB, HEART
	//these enums have ordinals, called by ordinal(), which returns the enum value.
	//enum values start from 0 and go higher to the right. ex: diamond is 0, spade 1, etc.
}
